# App Surfaces

Place physical surface runtimes here.

- `web/` is the current runnable Next.js surface
- `miniapp/` and `mobile/` are placeholder surfaces
- `desktop/` is a reserved surface slot for later expansion
