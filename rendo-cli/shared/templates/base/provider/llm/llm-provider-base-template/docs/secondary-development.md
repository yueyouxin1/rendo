# Secondary Development

When extending this provider, keep model/endpoint assumptions visible in files and manifests. Agents should be able to swap providers by reading one directory and one manifest, not by reverse-engineering hidden runtime behavior.
