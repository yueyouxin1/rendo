# Runtime Modes

This provider base supports `source` and `hybrid`.

- `source`: provider calls are handled entirely in host-owned code
- `hybrid`: the host project keeps control of the adapter while delegating part of execution or routing to managed services
