# Inheritance Boundaries

Derived provider templates may add concrete SDK bindings or routing logic, but should preserve explicit credentials, endpoint configuration, and host compatibility boundaries.
