{
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "check": "npm run lint && npm run typecheck && npm run test",
    "docker:up": "docker compose -f ops/docker/compose.yaml up --build",
    "docker:down": "docker compose -f ops/docker/compose.yaml down",
    "docker:logs": "docker compose -f ops/docker/compose.yaml logs -f",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "tsx src/packages/db/seed.ts"
  }
}
