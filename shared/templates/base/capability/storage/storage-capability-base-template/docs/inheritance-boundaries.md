# Inheritance Boundaries

Derived capability templates may add vendor-specific drivers or operational detail, but should preserve explicit install plans, env requirements, and runtime-mode semantics.
