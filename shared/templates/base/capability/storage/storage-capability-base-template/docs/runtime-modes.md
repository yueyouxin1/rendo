# Runtime Modes

This capability base supports `source`, `managed`, and `hybrid`.

- `source`: storage behavior is implemented locally
- `managed`: storage behavior depends on a managed platform or vendor runtime
- `hybrid`: the host project keeps some local control while delegating parts of storage handling
