# Authoring A Base Template

Use this feature core template when you need an official or internal feature base template.

## Minimum expectations

- Keep the root control-plane files stable.
- Introduce concrete feature structure only in the base template overlay.
- Record the new base template in `lineage.coreTemplate`.
