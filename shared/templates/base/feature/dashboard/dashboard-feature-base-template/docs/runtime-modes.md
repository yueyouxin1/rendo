# Runtime Modes

This feature base supports `source` and `hybrid`.

- `source`: all dashboard behavior lives in the host project
- `hybrid`: the host project still owns rendering, but some data or orchestration may come from managed services
