# Inheritance Boundaries

Derived feature templates may add richer widgets, routes, and data contracts, but should preserve the rule that a feature remains installable into a host starter without taking ownership of the whole application.
