# Structure

- `feature/README.md`: human-readable module contract
- `feature/widgets.json`: machine-readable widget slots and surface hints
- root control-plane files: manifest, project manifest, docs

## Design rule

This base template stays intentionally small. It defines the canonical feature-module boundary without assuming a specific host router, provider, or product domain.
