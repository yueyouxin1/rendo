# Application Base Starter Template

This starter proves that:

1. one base starter can be multi-surface-capable without generating every surface by default
2. `rendo create --surfaces ...` decides the physical surfaces for a concrete project
3. interface application starters can share a canonical base before deriving richer business starters

