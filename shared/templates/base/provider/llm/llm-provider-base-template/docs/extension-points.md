# Extension Points

- `src/README.md`
- `src/config.example.json`
- `integration/README.md`
- credential contract in `rendo.template.json`

## Host rule

Provider templates should remain installable and swappable. Keep host-facing adapter contracts explicit instead of letting vendor specifics leak into the host starter.
