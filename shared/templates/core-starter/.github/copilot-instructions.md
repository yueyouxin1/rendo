# Copilot Instructions

- Treat this repository as a Rendo Core Starter instance.
- Preserve headless-first behavior.
- Keep all changes compatible with `rendo.template.json` and `rendo.project.json`.
- Do not add domain-specific product UI.
- Document any new extension point under `docs/`.
