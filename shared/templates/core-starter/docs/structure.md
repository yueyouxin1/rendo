# Structure

## Layout

- `apps/runtime`: minimal runnable validation surface
- `packages/contracts`: contract types and manifest helpers
- `packages/config`: env parsing and config access
- `packages/domain`: runtime snapshot composition
- `packages/sdk`: local and managed adapters
- `packages/utils`: shared utility helpers

## Why this shape

The Core Starter defines how projects grow, not what product shape they become.
