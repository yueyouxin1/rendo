# Authoring A Base Template

Use this capability core template when you need an official or internal capability base template.

## Minimum expectations

- Keep the root control-plane files stable.
- Introduce concrete capability structure only in the base template overlay.
- Record the new base template in `lineage.coreTemplate`.
