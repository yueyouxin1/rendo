# __RENDO_PROJECT_NAME__

This project was initialized from the Rendo `capability-core-template`.

## What it is

- The core contract for capability templates
- A minimal, agent-readable workspace
- A stable substrate for building capability base templates

## What it is not

- Not a concrete capability implementation
- Not a UI choice
- Not a hard dependency on a specific platform backend

## Commands

```bash
npm install
npm run health
npm run check
```

## Next step

Use this template to author a capability base template, then let derived capability templates grow from that base.
