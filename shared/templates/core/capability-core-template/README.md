# __RENDO_PROJECT_NAME__

This project was initialized from the Rendo `capability-core-template`.

## What it is

- The core contract for `capability templates`
- A minimal, agent-readable workspace
- A stable substrate for building `capability templates` base templates

## What it is not

- Not a concrete product starter
- Not a vendor binding
- Not a hidden runtime

## Commands

```bash
npm install
npm run health
npm run check
```

## Next step

Use this template to author a `capability templates` base template, then let derived assets grow from that base.
