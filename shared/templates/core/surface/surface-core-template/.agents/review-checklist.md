# Review Checklist

- Keep the root structure aligned with `rendo.template.json`.
- Preserve `AGENTS.md`, `CLAUDE.md`, `.agents/`, `interfaces/`, `src/`, `tests/`, and `integration/`.
- Prefer additive base/derived changes over rewriting core semantics.
