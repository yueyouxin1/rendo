# Integration Guidance

Document host integration expectations here.

- Human guidance lives in this directory.
- Machine-readable integration behavior belongs in `rendo.template.json` under `assetIntegration`.
- Physical integration roots are selected by `assetIntegration.modes[].targetRoot`.
