# Authoring A Base Template

Use this provider core template when you need an official or internal provider base template.

## Minimum expectations

- Keep the root control-plane files stable.
- Introduce concrete provider structure only in the base template overlay.
- Record the new base template in `lineage.coreTemplate`.
