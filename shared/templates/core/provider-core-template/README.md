# __RENDO_PROJECT_NAME__

This project was initialized from the Rendo `provider-core-template`.

## What it is

- The core contract for provider templates
- A minimal, agent-readable workspace
- A stable substrate for building provider base templates

## What it is not

- Not a concrete provider integration
- Not a UI choice
- Not a hard dependency on a specific platform backend

## Commands

```bash
npm install
npm run health
npm run check
```

## Next step

Use this template to author a provider base template, then let derived provider templates grow from that base.
