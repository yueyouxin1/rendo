# Authoring A Base Template

Use this surface core template when you need an official or internal surface base template.

## Minimum expectations

- Keep the root control-plane files stable.
- Introduce concrete shell and interaction structure only in the base template overlay.
- Record the new base template in `lineage.coreTemplate`.
