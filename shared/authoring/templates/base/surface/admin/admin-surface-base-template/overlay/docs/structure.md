# Structure

- `surface/README.md`: human-readable shell contract
- `surface/slots.json`: machine-readable slot definition
- root manifest and docs: compatibility, install semantics, and inheritance notes

## Design rule

This base template defines how an admin-facing shell is mounted and extended. It does not own the host product domain.
