# Runtime Modes

This surface base supports `source` and `hybrid`.

- `source`: the host project owns the full shell implementation
- `hybrid`: the host still owns surface composition while some backing services may be managed
