# CLAUDE.md

## Working model

- This starter extends Core Starter with a small domain surface only.
- The runtime adds a `/hello` endpoint and a domain-specific snapshot field.
- Treat it as a contract validation asset, not as a production app template.
