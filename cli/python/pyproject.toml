[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rendo-cli-python"
version = "0.1.0"
description = "Python implementation of the Rendo CLI"
requires-python = ">=3.11"

[project.scripts]
rendo-python = "rendo_cli.__main__:main"
